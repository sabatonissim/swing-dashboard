# CLAUDE.md — Swing Desk: Project Knowledge Base

> **מסמך חי.** מעדכנים אותו בסיום כל משימה משמעותית.
> בכל שיחה חדשה: צרף קובץ זה + הקבצים הרלוונטיים. אין צורך בהיסטוריית צ'אט.

---

## 1. חזון ומטרה

**Swing Desk** הוא דשבורד מסחר פיננסי בזמן אמת, המיועד לטריידרים קצרי-טווח (swing trading). המטרה: לספק בממשק אחד את כל המידע הקריטי לקבלת החלטות מסחר — סריקת מניות טכנית ברמה גבוהה, חדשות מאקרו, גרפי סקטורים ומדדים, וכלי ניהול סיכונים — בלי לנווט בין עשרות מקורות.

**מודל עסקי עתידי:** אתר ציבורי עם פוטנציאל מוניטיזציה (מנויים, פרמיום).

**כתובת האתר הנוכחית:** https://swing-desk-tau.vercel.app *(ראה סעיף 16 אם עברת לדומיין/שרת אחר)*

---

## 2. ארכיטקטורה כללית

```
[GitHub Repository: sabatonissim/swing-dashboard]
         |
         ├── Vercel (Frontend)
         │     └── index.html  ← האתר הציבורי
         │
         └── Railway (Backend)
               ├── swing-dashboard  ← FastAPI API (תמיד דלוק)
               ├── PIPELINE-מניות  ← Cron: סריקה פתיחה+סגירה
               ├── PIPELINE-חדשות  ← Cron: כל 30 דקות
               └── Postgres         ← מסד הנתונים
```

**עיקרון:** שינוי קוד → push ל-GitHub → Railway ו-Vercel מתעדכנים אוטומטית.

**חשוב:** הארכיטקטורה הזו (Railway+Vercel) היא הבחירה הנוכחית, לא דרישה קשיחה. כל הקוד נכתב כך שיעבוד על כל ספק אחסון שתומך ב-Postgres + Python — ראה סעיף 16 למה בדיוק צריך לשנות אם תעבור.

---

## 3. קבצי הפרויקט

| קובץ | איפה רץ | תפקיד |
|---|---|---|
| `index.html` | Vercel (או כל אחסון סטטי) | הדשבורד — ממשק המשתמש המלא, קובץ יחיד |
| `api_server.py` | Railway (או כל שרת Python) | FastAPI — מחזיר נתונים מ-Postgres לפרונט |
| `pipeline_a_scanner.py` | Railway Cron (או כל cron) | סורק טכני — מזהה תבניות, כותב ל-Postgres |
| `pipeline_b_news_aggregator.py` | Railway Cron (או כל cron) | שולף חדשות RSS, מתרגם, כותב ל-Postgres |
| `requirements.txt` | כל השירותים | תלויות Python |

---

## 4. טכנולוגיות

### Frontend
- **Vanilla HTML/CSS/JS** — קובץ אחד, בלי framework, בלי build process
- **TradingView Widget** (חינמי) — גרפים בתוך המגירה וגרפי מדדים
- **Chart.js 4.5.0** (CDN עם גיבוי jsdelivr) — גרפי פונדמנטלס וגרף הסקטורים
- **RTL עברית כברירת מחדל**, עם toggle לאנגלית
- **localStorage** — משמש רק למחשבון הסיכונים (שווי תיק/אחוז סיכון אחרונים) ולהעדפת שפה. לא לשום נתון עסקי.

### Backend
- **FastAPI + Uvicorn** — Python API
- **psycopg2-binary** — חיבור ל-Postgres (⚠️ ראה סעיף 14 — זה היה חסר לגמרי מ-`requirements.txt` וגרם לקריסת דיפלוי מלאה)
- **yfinance** — נתוני שוק בזמן אמת (חינמי, ללא API key)
- **feedparser** — שליפת RSS
- **urllib (built-in)** — תרגום ל-Google Translate ללא API key

### Infrastructure (נוכחי)
- **Railway** — API + Cron Jobs + Postgres
- **Vercel** — אחסון הפרונט (חינמי)
- **GitHub** — source control + trigger לdeploy אוטומטי

### מה לא בשימוש (ולמה)
- ❌ **OpenAI** — הוסר לגמרי משני ה-pipelines. גם pipeline_b (תרגום חדשות) וגם pipeline_a (ניתוח סנטימנט) לא קוראים ל-OpenAI יותר — המשתמש לא רוצה לשלם על API בתשלום כרגע. תרגום חדשות חוזר ל-Google Translate החינמי (עם מילון מונחים מורחב), ו"למה זה חשוב" מבוסס על מיפוי סטטי לפי קטגוריה (`CATEGORY_HOOKS`) במקום יצירה דינמית.
- ❌ **lxml** — הוסר. נוסה לצורך שליפת רשימת S&P 500 מוויקיפדיה (`pandas.read_html`), אבל `lxml` יכולה להיכשל בקומפילציה מקוד מקור בזמן build ולהפיל את כל הדיפלוי. הוחלף במקור CSV פשוט (`datasets/s-and-p-500-companies` ב-GitHub) שנטען עם `pandas.read_csv` — בלי שום תלות התקנה נוספת.
- ❌ **SQLite** — הוחלף ב-Postgres כי Railway לא שומר קבצים מקומיים
- ❌ **deep-translator** — הוחלף ב-urllib ישיר (יותר אמין ב-Railway)
- ❌ **Node.js / CLI** — לא הותקן, כל העלאה היא דרך GitHub web UI

---

## 5. מסד הנתונים (Postgres)

### `scanned_stocks` — תוצאות הסריקה
עמודות עיקריות:
- `ticker`, `trigger_text_he/en`, `pattern_type` (מזהה קבוע לתבנית — לצורך סטטיסטיקת הצלחה, ראה סעיף 7)
- `swing_score` (0-100, כולל בונוסים/עונשים ממבנה מגמה, RS Rating, וקרבה לדוחות)
- `entry_price`, `support_level`, `resistance_targets` (JSON — רמות אמיתיות מנקודות מפנה, לא אחוז שרירותי)
- `sma50`, `sma150`, `sma200`, `trend_stage` (Stage 1-4 בשיטת Weinstein/Minervini)
- `atr_value`, `atr_pct` (Average True Range, 14 יום)
- `rs_rating` (1-99, עוצמה יחסית מול S&P 500 — ראה סעיף 7)
- `days_to_earnings` (עד/מאז דוחות קרובים)
- `forward_return_10d`, `forward_return_20d` — **ממולאים בדיעבד** ע"י `backfill_forward_returns()` ברגע שעברו 30+ יום מהאיתות, לצורך סטטיסטיקת הצלחה
- `ai_summary_he/en`, `breakout_volume_pct`, `change_pct`, `exchange`, `market_cap`, `avg_volume_20d`, `timestamp`

### `macro_news` — חדשות מאקרו
- `category_tag`, `summary_he`, `summary_en`, `hook_he` (משפט "למה זה חשוב", לפי קטגוריה — ראה סעיף 8), `impact_level` (High/Critical), `source_url` (**UNIQUE constraint** — מונע כפילויות), `timestamp`

### `watchlist` — רשימת מעקב אישית
- `ticker` (PRIMARY KEY), `added_at`

### `universe_movers` — גיבוי ל"הכי זזות"
- `ticker` (PRIMARY KEY), `change_pct`, `close_price`, `timestamp` — מתעדכן מכל הטיקרים שהסורק בודק (לא רק המסומנים), משמש כ-fallback אם ה-screener הכללי של יאהו נכשל

### `ui_strings`, `analytics_events` — כמו קודם, לא שינוי

### משתני סביבה נדרשים
- `SWING_DB_PATH` **או** `DATABASE_URL` = כתובת Postgres המלאה (`postgresql://...`) — כל שלושת קבצי ה-Python בודקים את שניהם, אז זה עובד עם כל ספק שמגדיר `DATABASE_URL` בשם הסטנדרטי (Railway, Render, Fly.io, Supabase וכו')
- `ALLOWED_ORIGINS` (חדש) = רשימת דומיינים מותרים ל-CORS, מופרדים בפסיקים. אופציונלי — אם לא מוגדר, נופל לברירת המחדל הנוכחית. **חובה לעדכן את זה אם עוברים דומיין/frontend host** (ראה סעיף 16).
- ~~`OPENAI_API_KEY`~~ — לא נדרש יותר, אף קובץ לא קורא לו

---

## 6. API Endpoints

Base URL הנוכחי: `https://swing-dashboard-production-438f.up.railway.app`

| Endpoint | Method | תיאור |
|---|---|---|
| `/api/health` | GET | בדיקת חיות + חיבור DB |
| `/api/stocks` | GET | מניות מסומנות מה-DB, כולל כל שדות המגמה/RS/ATR/דוחות |
| `/api/macro-news` | GET | חדשות מאקרו — Critical נשמר עד 72 שעות בלי קשר לנפח חדשות שוטפות; הגבלת מקסימום פריטים לכל קטגוריה כדי שקטגוריה בודדת לא תציף את כל הרשימה |
| `/api/lookup/{ticker}` | GET | נתוני שוק חיים (yfinance) |
| `/api/market-movers` | GET | הכי זזות היום — כל השוק האמריקאי, cache 3 דק', fallback ל-`universe_movers` |
| `/api/sector-performance` | GET | **חדש** — ביצועי 11 הסקטורים (XLY/XLK/XLE וכו') מנורמלים ל-0%, לפי טווח (1M/3M/YTD/1Y). שולף כל טיקר בנפרד (לא batch) כי `.history()` בודד הוכח אמין יותר מ-`yf.download()` מרוכז |
| `/api/pattern-stats` | GET | **חדש** — אחוז הצלחה + תשואה ממוצעת (10/20 יום) לכל סוג תבנית, מבוסס היסטוריית סריקה בפועל. תבניות עם פחות מ-5 מקרים לא מוצגות |
| `/api/watchlist` | GET/POST/DELETE | **חדש** — רשימת מעקב אישית. GET מחזיר שם חברה + מחיר + שינוי יומי + ATR% לכל טיקר |
| `/api/fundamentals/{ticker}` | GET | דוחות רבעוניים מ-SEC EDGAR, EPS מדולל + רצועת P/E היסטורית |
| `/api/stock-news/{ticker}` | GET | חדשות ספציפיות לטיקר (yfinance) |
| `/api/track` | POST | רישום event לאנליטיקס |
| `/api/analytics/top-interest`, `/api/analytics/summary` | GET | אנליטיקס |

---

## 7. Pipeline A — סורק מניות טכני

### לוח זמנים
- `30 13 * * 1-5` — פתיחת שוק (16:30 ישראל)
- `40 19 * * 1-5` — 20 דק' לפני סגירה (22:40 ישראל)

### Universe — **הורחב משמעותית**
במקום ~90 טיקרים קבועים: **S&P 500 המלא**, נשלף בזמן אמת מקובץ CSV ציבורי (`datasets/s-and-p-500-companies` ב-GitHub), ממוזג עם רשימת תוספות קבועה (ETFs כמו QQQ/IWM/ARKK שלא נכללים במדד). נופל חזרה לרשימה הקבועה בלבד אם השליפה נכשלת.

### פילטרי כניסה
מחיר > $10 | נפח 20 יום > 1.5M | שווי שוק > $1.5B | NYSE/NASDAQ בלבד

### היסטוריה נשלפת
**2 שנים** (לא 6 חודשים כמו קודם) — נדרש לחישוב ממוצע 200 יום + השיפוע שלו + RS Rating לשנה אחורה. *(⚠️ זה היה באג אמיתי: התכונה של "מבנה מגמה" נבנתה בעבר אבל מעולם לא עבדה בפועל כי לא הייתה מספיק היסטוריה בשליפה — תוקן.)*

### תבניות זיהוי (לפי עדיפות)
1. **Cup & Handle** (80+)
2. **Double Bottom (W)** (79+)
3. **52-Week High Breakout** (78+)
4. **Golden Cross** (74+)
5. **Bull Flag** (72+)
6. **Ascending Triangle** (68+)
7. **Ascending Trendline Support** (66) — **חדש**: מחיר "מקפץ" על קו מגמה עולה דרך שפלים גבוהים יותר (בניגוד לפריצת קו מגמה יורד) — תבנית המשכיות, לא פריצה
8. **MACD Bullish Crossover** (64+)
9. **Momentum Surge** (62+)
10. **RSI Oversold Bounce** (58+)
11. **Descending Trendline Break** (55+)

### מבנה מגמה (Trend Structure) — **חדש**
ממוצעים 50/150/200 יום + שיפוע + סדר, מסווגים ל-4 שלבים בשיטת Weinstein/Minervini:
- **Stage 2 - Uptrend**: מחיר מעל ממוצע 50 מעל 150 מעל 200, כולם בסדר עולה, ממוצע 150 עולה → **בונוס של +8 לציון**
- **Stage 4 - Downtrend**: מחיר מתחת לממוצעים היורדים → **עונש של -20**
- **Stage 3 - Topping**: היפוך אפשרי → **עונש של -10**
- **Stage 1 - Basing**: בסיס/איחוד, ללא התאמה

### RS Rating (עוצמה יחסית) — **חדש**
סולם 1-99 בסגנון IBD/O'Neil: תשואה משוקללת מול S&P 500 (SPY) ב-3/6/9/12 חודשים אחרונים (משקל גבוה לתקופה האחרונה), מדורגת כאחוזון מול **כל שאר המניות שנסרקו באותה ריצה**. RS 90+ → **בונוס +8**, RS 60-79 → **בונוס +3**, RS מתחת ל-40 → **עונש -15**.

### מודעות לדוחות כספיים — **חדש**
`info["earningsTimestamp"]` (כבר חלק מהמידע הנשלף ממילא — אין קריאת API נוספת). אם דוחות תוך 7 ימים → **עונש -12** + אזהרה מפורשת בטקסט (⚠️).

### סטטיסטיקת הצלחה לתבניות — **חדש**
`backfill_forward_returns()` רץ בתחילת כל סריקה: מוצא איתותים ישנים (30+ יום) בלי תוצאה ידועה עדיין, שולף את המחיר 10/20 ימי מסחר אחרי האיתות, ושומר את התשואה. `/api/pattern-stats` מציג את הסטטיסטיקה המצטברת. **דורש זמן להצטבר — אין נתונים משמעותיים בשבועות הראשונים.**

### ATR (Average True Range)
מחושב לכל מניה (14 יום), מוצג גם כערך דולרי וגם כאחוז מהמחיר.

### תמיכה/התנגדות — **תוקן**
במקום `close × 1.05/1.10` שרירותי (כמו שהיה קודם): רמות אמיתיות מנקודות מפנה (swing highs/lows) בהיסטוריית המחיר.

---

## 8. Pipeline B — אגרגטור חדשות

### לוח זמנים
`*/30 * * * 1-5` — כל 30 דקות בימי חול

### מקורות RSS — **עודכן**
Reuters (Top/Business) המקוריים **מתים** (Reuters סגרה פידי RSS ציבוריים ב-2020) — הוחלפו בחיפוש Google News מסונן ל-reuters.com. נוספו גם **שני פידי חיפוש רחבים של Google News** (מאקרו/שווקים, גאופוליטיקה) שמרכזים ידיעות מעשרות מקורות — זה מה שפותר בעיית "אין מספיק חדשות" אם מקור בודד נופל לשקט. נשארו: CNBC (Top/Markets/World), Yahoo Finance, Seeking Alpha, MarketWatch, Investing.com.

### מניעת כפילויות — **חדש**
`UNIQUE constraint` על `source_url` + `ON CONFLICT DO NOTHING`. לפני זה, אותה כתבה חוזרת הוכנסה כשורה חדשה בכל ריצה, והציפה את ה-"25 האחרונות" עם אותם סיפורים שוב ושוב. ⚠️ **הוספת ה-constraint על טבלה שכבר מכילה כפילויות מהעבר גורמת לשגיאה** — לכן `init_db()` מנקה כפילויות ישנות (הופך `source_url=''` ל-NULL, מוחק שורות כפולות ישנות) *לפני* יצירת ה-constraint, בכל ריצה (לא-פעולה אחרי הפעם הראשונה).

### חדשות "Critical" נשארות יותר זמן — **חדש**
`/api/macro-news` שומר איתותי Critical (החלטות ריבית, מלחמות וכו') עד **72 שעות**, בלי קשר לכמה עדכונים שוטפים נכנסו מאז. חדשות "High" רגילות ממשיכות להתחלף כרגיל, עם הגבלת מקסימום פריטים לכל קטגוריה (כדי שקטגוריה בודדת לא תציף את כל הרשימה).

### תרגום — **הוחזר לחינמי, בכוונה**
בלי OpenAI (המשתמש לא רוצה לשלם). Google Translate חינמי + מילון מונחים מורחב (~60 מונחי מאקרו/פייננס עם תרגום קבוע ונכון, במקום לסמוך על ניחוש התלוי-הקשר של Google).

### "למה זה חשוב" (hook_he) — **חדש**
במקום קריאת AI לכל כתבה: מיפוי סטטי `CATEGORY_HOOKS` — 20 הסברים ממוקדים לפי קטגוריה (ריבית/אינפלציה/גאופוליטי/דוחות/שבבים וכו'), חינמי לגמרי.

---

## 9. Frontend — index.html

### מבנה הדף
1. **Header** — לוגו, חיפוש, כפתור שפה, שעון
2. **Breaking News Bar** — חדשות קריטיות בלבד
3. **Market Charts + Movers strip**:
   - טאב **מדדים/סקטורים** (`מדדים` — SPY/QQQ/GLD/IWM/Bitcoin; `סקטורים` — גרף Koyfin-style מנורמל של 11 סקטורי SPDR עם לגנד ניתן-ללחיצה + טווחי זמן 1M/3M/YTD/1Y)
   - עמודת "movers" עם טאב משלה: **הכי זזות** / **רשימת מעקב** (עם כפתור + להוספה ישירה)
4. **Split Board:** מניות (סריקה אחרונה + ימים קודמים) / חדשות + פילטר סקטורים
5. **Drawer** — מגירה צדדית: מניה (גרף + מבנה מגמה/RS/ATR/דוחות + פונדמנטלס + חדשות + השוואה + מחשבון סיכונים) / חדשות

### תכונות עיקריות שנוספו בסבב הזה
- **השוואת שתי מניות** — טבלה ממוקדת (לא שני drawers מלאים) עם מגמת 4 רבעונים אחרונים לכל מדד, וקישור "פתח מלא" לכל טיקר
- **רשימת מעקב** — כפתור + לפתיחת prompt הזנת טיקר, כוכב ⭐ בכרטיס מניה כנוחות נוספת, שורות עם שם חברה + ATR%
- **מחשבון גודל פוזיציה** — חישוב טהור בצד לקוח (אין קריאת API), מבוסס על שווי תיק **נוכחי** שמוזן מחדש בכל שימוש (לא ערך שמור), אחוז סיכון, כניסה/סטופ/יעד
- **תוויות הסבר (i)** על כל נתון פונדמנטלי + RS Rating — לחיצה מסבירה מה הנתון אומר
- **הערת מקור** — קישור ל-SEC EDGAR (הדוחות הגולמיים) כדי שהמשתמש יוכל לוודא דיוק בעצמו

### שפה
ברירת מחדל עברית RTL, toggle שומר ב-localStorage, כל נתוני ה-API מגיעים עם שדות `_he`/`_en`.

### API Connection
```javascript
const API_BASE = "https://swing-dashboard-production-438f.up.railway.app";
```
⚠️ זו השורה היחידה שצריך לשנות אם עוברים ספק אחסון לבקאנד — ראה סעיף 16.

---

## 10. החלטות ארכיטקטורה מרכזיות

| החלטה | סיבה |
|---|---|
| Vanilla JS (לא React) | פשטות, קובץ אחד, אין build process |
| Postgres (לא SQLite) | לא כל אחסון שומר קבצים מקומיים בין deployments |
| yfinance (לא API בתשלום) | חינמי, מספיק ל-MVP |
| urllib לתרגום, בלי OpenAI | עלות — המשתמש לא רוצה לשלם על API כרגע בשום pipeline |
| S&P 500 מ-CSV (לא Wikipedia+lxml) | lxml עלולה להיכשל בהתקנה ולהפיל דיפלוי שלם; CSV לא דורש שום ספריית parsing נוספת |
| `.history()` בלולאה (לא `yf.download()` מרוכז) לנתוני סקטורים | קריאה מרוכזת אחת שנכשלת מפילה את כל 11 הסקטורים ביחד; לולאה סובלת כשל של טיקר בודד |
| CORS דרך משתנה סביבה | מעבר דומיין/אחסון לא ידרוש שינוי קוד |
| GitHub כ-source of truth | כל push מעדכן את שני השירותים אוטומטית |

---

## 11. פיצ'רים שהושלמו ✅

**ליבה (מסבבים קודמים):** דשבורד split-screen, 11 תבניות טכניות, RSS מ-8+ מקורות, גרף TradingView, חיפוש חופשי, Breaking News Bar, גרפי מדדים, פילטר סקטורים בחדשות, toggle שפה, אנליטיקס, disclaimer, cron jobs, פונדמנטלס מ-SEC EDGAR עם תוויות הסבר.

**מהסבב הזה:**
- [x] תיקון קריסת deploy: `psycopg2` היה חסר לגמרי מ-`requirements.txt` (⚠️ ראה סעיף 14)
- [x] גרף סקטורים בסגנון Koyfin (11 ETF-ים, נורמליזציה, לגנד ניתן-ללחיצה, טווחי זמן)
- [x] השוואת שתי מניות עם מגמת 4 רבעונים
- [x] רשימת מעקב אישית (DB, לא localStorage) עם כפתור + והצגת שם/ATR
- [x] מחשבון גודל פוזיציה (חישוב צד-לקוח בלבד)
- [x] מבנה מגמה (SMA50/150/200 + שלבי Weinstein) — ותיקון הבאג שמנע ממנו לעבוד מלכתחילה
- [x] RS Rating (1-99, עוצמה יחסית מול S&P 500) + תוית הסבר
- [x] מודעות לתאריכי דוחות עם עונש ניקוד
- [x] סטטיסטיקת הצלחה לתבניות (win-rate + תשואה ממוצעת, מבוסס היסטוריה אמיתית)
- [x] הרחבת Universe הסריקה מ-~90 ל-S&P 500 המלא
- [x] תיקון תרגום חדשות: מילון מונחים מורחב + "למה זה חשוב" סטטי לפי קטגוריה (בלי OpenAI)
- [x] מניעת כפילויות בחדשות + שמירת חדשות Critical עד 72 שעות + הגבלת גיוון קטגוריות
- [x] תיקון באג SPCX (מדדים→סקטורים→חזרה למדדים היה פותח טיקר שגוי)
- [x] עיצוב כפתורי טוגל (מדדים/סקטורים) שהיו בלי CSS בכלל
- [x] תוויות ציר Y בכל הגרפים הועברו לימין (RTL)
- [x] CORS דרך משתנה סביבה לפורטביליות
- [x] מודל מקורות מידע/הבהרה (ⓘ בכותרת) — מסביר בפירוט מאיפה כל נתון מגיע (מחירים מ-Yahoo Finance, פונדמנטלס מ-SEC EDGAR, חדשות מ-RSS, הסריקה הטכנית אינה ייעוץ השקעות)
- [x] תיקון RTL בגרפי TradingView — `direction:ltr` על מכולי הגרף מנע מהם להיפתח גלולים לנתונים ישנים במקום לנקודה העדכנית ביותר
- [x] קיפול/פתיחה של קבוצות "סריקה אחרונה"/"ימים קודמים" ברשימת המניות (חץ ליד כל כותרת קבוצה)

---

## 12. פיצ'רים חלקיים / בעיות ידועות ⚠️

| בעיה | פירוט | פתרון מוצע |
|---|---|---|
| VIX לא עובד | ניסינו CBOE:VIX, TVC:VIX, INDEX:VIX — כולם נכשלים ב-widget חינמי | להחליף ל-UVXY (ETF) |
| פילטר סקטורים בחדשות | סקטור נלקח מ-category_tag, לא שדה ייעודי | להוסיף שדה sector ל-Pipeline B |
| DB ריק בהתחלה | עד הסריקה הראשונה בשעות מסחר — מוצגים נתוני דמו | תקין, ידוע |
| סטטיסטיקת תבניות ריקה בהתחלה | דורש 30+ יום לפני שיש תוצאות ידועות | תקין, ידוע — יתמלא עם הזמן |

---

## 13. TODO עתידי 🔮

### עדיפות גבוהה
- [ ] החלף VIX ב-UVXY
- [ ] הוסף שדה `sector` אמיתי ל-macro_news

### עדיפות בינונית
- [ ] עמוד Deep Dive מלא לכל מניה
- [ ] Push notifications לפריצות
- [ ] Fear & Greed Index
- [ ] חיפוש חדשות

### עתיד רחוק
- [ ] OpenAI לסיכומים חכמים (כשיש הכנסה — לא לפני)
- [ ] Reddit/X social sentiment
- [ ] גרסת פרמיום + משתמשים מרובים (הרשימת מעקב הנוכחית היא רשימה יחידה משותפת, לא per-user)

---

## 14. מגבלות ידועות ובאגים מתועדים

| מגבלה/באג | פירוט |
|---|---|
| **`psycopg2` חסר מ-requirements.txt** | הקריסה הכי חמורה שקרתה: הקובץ מעולם לא הכיל את `psycopg2` (רק `psycopg2-binary` נוסף בסבב הזה), למרות ששלושת קבצי ה-Python תלויים בו. ה-build עבד במקרה בזכות cache ישן, עד שbuild נקי חשף את זה וקרס הדיפלוי כולו. **לקח:** אחרי כל שינוי ב-requirements.txt, לוודא שכל import בקוד מכוסה. |
| **`lxml` בתלויות = סיכון קריסת build** | נוסה זמנית לפרסור HTML של ויקיפדיה, הוסר כי ספריות שדורשות קומפילציה מקוד מקור (כמו lxml) יכולות להיכשל תלוי בסביבת ה-build ולהפיל את כל הדיפלוי — לא רק את התכונה שהשתמשה בהן. הועדף מקור CSV פשוט במקום. |
| yfinance rate limit | timeout קשיח (15 שנ') + השהיה 0.3 שנ' בין טיקרים + circuit breaker אחרי 8 timeouts רצופים |
| Google Translate חינמי | לא רשמי, יכול להיחסם — fallback: כותרת באנגלית |
| CORS | דרך `ALLOWED_ORIGINS` (env var) — ברירת מחדל: swing-desk-tau.vercel.app + localhost |
| טיקר SQ→XYZ | Block Inc שינתה טיקר ב-2025 — מעודכן ב-universe הקבוע |
| numpy → psycopg2 | `_native()` ממיר כל טיפוס numpy ל-Python רגיל לפני INSERT — מונע קריסת סריקה מטיפוס לא-נתמך |
| macro_news כפילויות | UNIQUE constraint על source_url + ניקוי כפילויות ישנות לפני יצירת ה-constraint (ראה סעיף 8) |
| RTL הופך גרפי TradingView | הדף כולו `dir="rtl"`, וזה יורש למכולי ה-iframe של TradingView — למרות שזמן בגרף תמיד זורם משמאל לימין בלי קשר לשפת הדף. גרם לגרף להיפתח גלול לנתונים ישנים (התחלת הטווח) במקום לנקודה העדכנית ביותר. תוקן עם `direction:ltr` מפורש על `.tv-chart-box` ו-`#market-tv-chart`. |

---

## 15. איך לפתוח שיחה חדשה

1. צרף קובץ זה (`CLAUDE.md`)
2. צרף את הקוד הרלוונטי (`index.html` / `pipeline_a_scanner.py` וכו')
3. תאר מה אתה רוצה לשנות
4. Claude ימשיך מיד ללא היסטוריית שיחה

**לעדכון הקובץ:** בקש "עדכן את CLAUDE.md" בסיום כל שינוי משמעותי.

---

## 16. מעבר לדומיין/שרת אחר

הקוד נכתב כך שרוב הפרויקט **לא** דורש שינוי במעבר לספק אחר — רק כמה נקודות ספציפיות:

### חובה לשנות
1. **`index.html` → `API_BASE`** (שורה מסומנת ב-⚠️ בקוד): לעדכן לכתובת שבה `api_server.py` רץ אחרי המעבר.
2. **`api_server.py` → `ALLOWED_ORIGINS`**: להגדיר כמשתנה סביבה בשרת החדש עם כתובת ה-frontend החדשה (רשימה מופרדת בפסיקים). בלי זה, הדפדפן יחסום את הבקשות עם שגיאת CORS גם אם ה-API עצמו עובד.
3. **`DATABASE_URL`/`SWING_DB_PATH`**: להגדיר כמשתנה סביבה בשרת החדש עם כתובת ה-Postgres החדש. כל שלושת קבצי ה-Python כבר קוראים את שני השמות, אז זה עובד עם כל ספק שמגדיר `DATABASE_URL` (כמעט כולם: Render, Fly.io, Supabase, Heroku).
4. **פקודת ההרצה** (Procfile / railway.json / start command, תלוי בפלטפורמה): `uvicorn api_server:app --host 0.0.0.0 --port $PORT` לשרת ה-API, ותזמון cron מקביל להרצת `pipeline_a_scanner.py`/`pipeline_b_news_aggregator.py`.

### לא צריך לשנות
- שום דבר ב-yfinance / SEC EDGAR / RSS — כולם endpoints ציבוריים חינמיים, לא קשורים לספק האחסון
- שום דבר בקוד ה-DB schema — Postgres רגיל, לא ספציפי ל-Railway
- שום דבר ב-`requirements.txt`

### הזדמנות, לא חובה
כמה תיקוני עמידות בקוד (timeout/retry/backoff על yfinance ו-SEC, שימוש ב-`.history()` בלולאה במקום `yf.download()` מרוכז) נבעו מהתנהגות ספציפית שנצפתה מ-IP של Railway. ייתכן ששרת אחר לא יסבול מאותה בעיה כלל — שווה לבדוק אחרי מעבר אם אפשר לפשט חזרה, אבל אין סיבה למהר, הקוד הנוכחי עמיד בכל מקרה.

---

*עדכון אחרון: אוגוסט 2026 | גרסה: MVP v2.1*
