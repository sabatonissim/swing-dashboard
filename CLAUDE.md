# CLAUDE.md — Swing Desk: Project Knowledge Base

> **מסמך חי.** מעדכנים אותו בסיום כל משימה משמעותית — רק כשמתבקש במפורש ("עדכן את CLAUDE.md").
> בכל שיחה חדשה: צרף קובץ זה. הקבצים עצמם אפשר למשוך ישירות מ-GitHub (הריפו ציבורי) — אין צורך להעלות אותם ידנית, ראה סעיף 14.

---

## 1. חזון ומטרה

**Swing Desk** הוא דשבורד מסחר פיננסי בזמן אמת, המיועד לטריידרים קצרי-טווח (swing trading), עם דגש על **ניתוח טכני מבוסס תבניות מחיר אמיתיות** — לא רק אינדיקטורים גנריים. המטרה: לספק בממשק אחד את כל המידע הקריטי לקבלת החלטות מסחר — סריקת מניות טכנית ברמה גבוהה, חדשות מאקרו, גרפי סקטורים ומדדים, יומן דוחות, בדיקה היסטורית (Backtesting), וניתוח מעמיק למניה בודדת — בלי לנווט בין עשרות מקורות.

**יעד נוכחי:** שימוש אישי, ברמה מקצועית ואמינה. **לא** מוצר רב-משתמשים/בתשלום כרגע — מעבר למודל כזה (הרשמה, חיוב, רישוי מסחרי לנתוני Yahoo) נדחה במפורש עד שהיעד ישתנה בפועל.

**כתובת האתר הנוכחית (Frontend):** https://swing-desk-tau.vercel.app
**כתובת ה-API הנוכחית (Backend):** https://swing-dashboard-3btx.onrender.com

---

## 2. ארכיטקטורה כללית

```
[GitHub Repository: sabatonissim/swing-dashboard — PUBLIC]
         |
         ├── Vercel (Frontend)
         │     └── index.html
         │
         ├── Render (Backend API — Free Web Service)
         │     └── api_server.py — FastAPI, "נרדם" אחרי 15 דק' חוסר פעילות
         │
         ├── Neon (Postgres — Free Tier)
         │     └── מסד הנתונים — "auto-suspend" אחרי ~5 דק' חוסר פעילות ב-DB
         │
         └── GitHub Actions (מחליף את ה-Cron Jobs של Railway — חינמי ללא הגבלה, כי הריפו ציבורי)
               ├── stock-scanner.yml          — מריץ pipeline_a_scanner.py, 2 טריגרים לכל סשן (ראשי + גיבוי ~15 דק' אחריו)
               ├── news-aggregator.yml        — מריץ pipeline_b_news_aggregator.py
               ├── keep-render-awake.yml      — פינג ל-/api/health כל 13 דק' כדי ש-Render לא ירדם
               └── cleanup-stale-signals.yml  — ידני בלבד (workflow_dispatch), מריץ cleanup_stale_signals.py
```

**עיקרון:** שינוי קוד → push ל-GitHub → Render ו-Vercel מתעדכנים אוטומטית (build+deploy). קבצי ה-workflows רצים לפי לוח הזמנים שמוגדר בהם, בלי תלות בשרת חי.

### ⚠️ למה הריפו ציבורי (החלטה מכוונת, לא שגיאה)
GitHub Actions מגביל ריפו **פרטי** ל-2,000 דקות ריצה/חודש בחינם. ריפו **ציבורי** מקבל דקות **ללא הגבלה**. הסודות (`DATABASE_URL` וכו') לא נחשפים — שמורים ב-GitHub Secrets מוצפנים, בנפרד לגמרי מהקוד.

### ⚠️ תקלת CORS אמיתית שקרתה — לזכור לעתיד
פעם אחת האתר נשאר תקוע לצמיתות על "מתחבר לשרת..." **למרות** שלוגי Render הראו בקשות מצליחות (200 OK). הסיבה: הבקשות המוצלחות בלוג היו רק מפינג ה-keep-alive האוטומטי (IP קבוע, קצב קבוע) — לא מהדפדפן בפועל. הדפדפן קיבל שגיאת **CORS** (`No 'Access-Control-Allow-Origin' header`), כי ה-deploy האחרון ב-Render **לא באמת עלה** (נתקע/נכשל בשקט) והשרת המשיך להריץ קוד ישן עם קונפיגורציית CORS שונה — **גם כשה-`ALLOWED_ORIGINS` וגם ברירת המחדל בקוד היו תקינים לגמרי**. **אבחון:** F12 → Console בדפדפן (לא רק לוגי Render!) → לחפש שגיאת CORS. **תיקון:** Render → Deploys → "Deploy latest commit" ידנית כדי לוודא שהקוד העדכני באמת רץ.

### ⚠️ קובץ workflow חייב היכן שהוא רץ, לא היכן שהוא מתוזמן
טעות שקרתה בפועל: קובץ `.yml` (התזמון) חייב להיות בתוך `.github/workflows/`, אבל כל סקריפט Python שהוא מריץ (למשל `cleanup_stale_signals.py`) חייב להיות **בשורש הריפו** — לא בתוך תיקיית ה-workflows עצמה. שגיאה כמו `can't open file '.../cleanup_stale_signals.py': No such file or directory` בלוג של GitHub Actions היא כמעט תמיד זה בדיוק.

---

## 3. קבצי הפרויקט

| קובץ | איפה רץ | תפקיד |
|---|---|---|
| `index.html` | Vercel | הדשבורד — ממשק המשתמש המלא, קובץ יחיד |
| `api_server.py` | Render | FastAPI — מחזיר נתונים מ-Postgres/yfinance/SEC לפרונט |
| `pipeline_a_scanner.py` | GitHub Actions (`stock-scanner.yml`) | סורק טכני — מזהה תבניות, יומן דוחות, כותב ל-Postgres |
| `pipeline_b_news_aggregator.py` | GitHub Actions (`news-aggregator.yml`) | שולף חדשות RSS, מסווג, מתרגם, כותב ל-Postgres |
| `cleanup_stale_signals.py` | GitHub Actions (`cleanup-stale-signals.yml`, **ידני בלבד**) | סקריפט תחזוקה חד-פעמי — מכווץ רצפי איתותים כפולים/ישנים ב-`scanned_stocks` |
| `requirements.txt` | Render + GitHub Actions | תלויות Python |
| `.github/workflows/*.yml` | GitHub Actions | תזמון סריקה/חדשות/keep-alive/ניקוי |

---

## 4. טכנולוגיות

### Frontend
- **Vanilla HTML/CSS/JS** — קובץ אחד, בלי framework, בלי build process
- **TradingView Widget** — גרפים בכל מקום (בית, מגירת חיפוש, ניתוח מניה), עם **ממוצע נע פשוט 150 יום אדום** (`studies:["MASimple@tv-basicstudies"]`) על כל שלושת המופעים
- **Chart.js 4.5.0** — גרפי פונדמנטלס, סקטורים, equity curve, פילוח תקופתי
- **Treemap מותאם-אישית (vanilla JS, squarified algorithm), דו-שכבתי** — מפת החום מקובצת לפי סקטור GICS (סקטורים ממודדים לפי שווי שוק מצטבר, מניות בתוך כל סקטור לפי שווי שוק אינדיבידואלי), ראה סעיף 9
- **RTL עברית קבוע תמיד** (`dir="rtl"` לא משתנה יותר בהחלפת שפה — ראה תקלה קריטית בסעיף 9), toggle משנה רק טקסט
- **localStorage בשימוש** ל: שפה, פילטר קטגוריות חדשות (multi-select persisted) — לא לנתונים עסקיים כבדים

### Backend
- **FastAPI + Uvicorn**
- **psycopg2-binary** — Postgres
- **yfinance** — נתוני שוק (`.history()` אמין; `.info` לא אמין מ-Render — ראה סעיף 12)
- **SEC EDGAR API ישיר** (`data.sec.gov`) — מקור אמין למכפילים/שווי שוק/EPS כש-`.info` חסום; גם fallback לתיאור חברה
- **Wikipedia REST API** — fallback לתיאור חברה עשיר יותר (חינמי, בלי API key)
- **feedparser** — RSS
- **pandas.read_csv** — S&P 500 + סקטור/תת-סקטור GICS (מקור אמין, לא `.info`)
- **תרגום חינמי** (Google Translate "gtx" הלא-רשמי + MyMemory כגיבוי) — עם jitter+retry לאמינות, ומנגנון תיקון-עצמי לכתבות שנתקעו באנגלית (ראה סעיף 8)

### Infrastructure
- **Render** — Free Web Service ל-API
- **Neon** — Postgres חינמי לצמיתות, auto-suspend אחרי ~5 דק'
- **Vercel** — הפרונט
- **GitHub** — ציבורי, + Actions למחליף cron

### מה לא בשימוש (ולמה)
- ❌ **OpenAI/Anthropic API** — לא בשימוש בשום pipeline. חיבור AI לסיווג/סיכום חדשות נשקל כשדרוג עתידי (עלות אגורות בודדות עם מודל זול), **נדחה במפורש** עד שהאתר יהפוך למוצר אמיתי — כרגע חינמי בלבד. **חשוב:** מנוי Claude (Pro/Max) הוא ביחס חיוב **נפרד לגמרי** מ-Claude API — מנוי לא נותן גישת API לחיבור לאתר.
- ❌ **VIX** — הוסר לגמרי, Fear & Greed משמש תחליף
- ❌ **טוויטר/X** — נשקל (חשבונות כמו Earnings Whispers/Live Squawk לחדשות מהירות), **נדחה** — X API איבד את הטיר החינמי (פברואר 2026, pay-per-use ~$0.005/קריאה), נבחר במקום זאת יומן דוחות מבוסס yfinance (ראה סעיף 7)
- ❌ **cron-job.org / UptimeRobot / שירותי פינג חיצוניים** — נשקל כמה פעמים, נדחה כל פעם ע"י המשתמש (מעדיף למזער הרשמות/תשלומים)

---

## 5. מסד הנתונים (Postgres, Neon)

### ⚠️ נקודות קריטיות תפעוליות
- **Neon branches**: הנתונים ב-branch `import-2026-08...`, לא ב-`production`/`main`. חשוב לזכור בכל שינוי DB.
- **Neon auto-suspend**: מכבה חישוב אחרי ~5 דק' חוסר פעילות. כל קוד שעושה עבודה ארוכה לפני כתיבה ל-DB **חייב לפתוח חיבור בסמוך לכתיבה**, לא מראש.

### `scanned_stocks` — תוצאות הסריקה
עמודות עיקריות: `ticker`, `trigger_text_he/en`, `pattern_type`, `swing_score` (0-100), `entry_price`, `support_level`, `resistance_targets` (JSON), `sma50/150/200`, `trend_stage`, `atr_value/pct`, `rs_rating` (1-99), `days_to_earnings`, `forward_return_10d/20d`, `ai_summary_he/en`, `breakout_volume_pct`, `change_pct`, `exchange`, `market_cap`, `avg_volume_20d`, `timestamp`.
`/api/stocks` מחזיר **שורה אחת לכל טיקר** (המופע העדכני, `DISTINCT ON`) + `repeat_count_14d`.

**⚠️ ניקוי היסטורי שבוצע בפועל:** לפני תיקון ה-freshness-check (סעיף 7), תבניות פריצה היו "נדבקות" ומסתמנות מחדש בכל סריקה כל עוד המחיר נשאר מעל הרמה — במקרים מסוימים במשך שבועות/חודשים. `cleanup_stale_signals.py` רץ פעם אחת בפועל ומחק **10,664 מתוך 12,914 שורות** (82%) שהיו חזרות ישנות של אותו איתות. ההיסטוריה נצברת מחדש נקייה מעכשיו והלאה.

### `universe_movers` — heatmap + fallback ל"הכי זזות"
`ticker` (PK), `change_pct`, `close_price`, `in_sp500`, `in_nasdaq100`, `market_cap`, `timestamp`.

### `earnings_calendar` — יומן דוחות שבועי (חדש)
`ticker`, `report_date`, `session` (`bmo`/`amc`/`unknown`), `market_cap`, `eps_estimate`, `eps_actual`, `surprise_pct`, `revenue_estimate`, `revenue_actual`, `revenue_surprise_pct`, `updated_at`. `PRIMARY KEY (ticker, report_date)`.
**⚠️ עדכון מיוחד:** `revenue_estimate` נשמר ב-`UPSERT` עם `COALESCE` שמעדיף את הערך ה**ישן** (לא את החדש) — כי ה-"0q" (רבעון קרוב) של yfinance מתגלגל קדימה לרבעון הבא ברגע שהחברה מדווחת. שליפה מחדש אחרי הדוח הייתה נותנת בשקט מספר שגוי. לעומת זאת `eps_actual`/`revenue_actual` מעדיפים תמיד את הערך החדש (`COALESCE(EXCLUDED.x, table.x)`).

### `sec_cik_cache`
שומרת את מיפוי טיקר→CIK של SEC ב-Postgres — cold start של Render לא מוריד מחדש קובץ מ-SEC בכל פעם.

### `watchlist`
`ticker` (PK), `added_at`.

### משתני סביבה נדרשים
- `DATABASE_URL` — Neon branch `import-2026-08...`
- `ALLOWED_ORIGINS` — `https://swing-desk-tau.vercel.app` (Render Environment)

---

## 6. API Endpoints עיקריים

| Endpoint | תיאור |
|---|---|
| `/api/health` | בדיקת חיות + DB |
| `/api/stocks` | מניות מסומנות — dedup לפי טיקר + `repeat_count_14d` |
| `/api/macro-news`, `/api/macro-news/search` | חדשות מאקרו + חיפוש DB אמיתי |
| `/api/lookup/{ticker}` | נתוני שוק חיים; `market_cap`/`pe_ratio` נופלים ל-fundamentals (SEC); `description` נופל ל-Wikipedia ואז SEC |
| `/api/sector-comparison/{ticker}?period=1M\|3M\|YTD\|1Y` | תשואת המניה מול ETF הסקטור **וגם** ETF תת-הסקטור המדויק, עד 4 מתחרי תת-סקטור עם P/E + שווי שוק + דירוג-זולות |
| `/api/fundamentals/{ticker}` | דוחות SEC EDGAR — `trailing_pe`/`forward_pe` (LTM/NTM), `market_cap` (תגי `us-gaap` **ו-`dei`**), FCF/Revenue/NI + `_ltm` |
| `/api/heatmap?index=sp500\|nasdaq100` | מחזיר `sector` (GICS) לכל טיקר — הפרונט בונה טריטמאפ דו-שכבתי |
| `/api/earnings-calendar` | **חדש** — יומן דוחות השבוע, 40 השמות הגדולים ביותר שמדווחים בחלון של -2/+7 ימים, כולל EPS+הכנסות בפועל מול תחזית ברגע שיש |
| `/api/stock-news/{ticker}?lang=he\|en` | **עודכן** — חדשות חיות למניה ספציפית; `lang=he` מתרגם חינמי on-demand (קאש בזיכרון) |
| `/api/backtest?pattern=...&horizon=10\|20` | equity curve (**10% מההון לכל איתות, לא all-in**), drawdown, פילוח RS, `monthly_breakdown`/`quarterly_breakdown` |
| `/api/backtest/patterns` | רשימת תבניות עם איתותים בשלים |
| `/api/pattern-stats` | win rate + תשואה ממוצעת לכל תבנית |
| `/api/price-compare` | סדרת מחירים מנורמלת לאחוזים, עד 3 טיקרים |
| `/api/signal-history/{ticker}` | היסטוריית איתותים לטיקר |
| `/api/market-movers`, `/api/sector-performance`, `/api/fear-greed`, `/api/watchlist` | ללא שינוי מהותי |

---

## 7. Pipeline A — סורק מניות טכני

### לוח זמנים
`stock-scanner.yml`: **2 טריגרים לכל סשן** — ראשי בדקה לא-עגולה + גיבוי כ-15 דק' אחריו (16:37/16:52 ו-22:47/23:02 שעון ישראל בקירוב) — כי GitHub Actions `schedule` לא מדויק, במיוחד בדקות עגולות עמוסות. `should_skip_scan()` מונע סריקה כפולה אם שני הטריגרים בכל זאת יורים קרוב זה לזה (בודק שעברו לפחות 90 דק' מהסריקה האחרונה). **לעדכן ידנית מסביב לסוף אוקטובר/מרץ** (שינוי שעון קיץ/חורף).

### Universe
S&P 500 + Nasdaq 100 מלאים + רשימת תוספות. פילטרים: מחיר > $10, נפח 20 יום > 1.5M, שווי שוק > $1.5B, NYSE/NASDAQ.
**`build_scan_universe()` מסובב את סדר הרשימה לפי היום בשנה** — כי אם Yahoo חוסם באמצע סריקה (circuit breaker `MAX_CONSECUTIVE_TIMEOUTS`), סדר קבוע היה מרעיב תמיד את אותם טיקרים "בסוף" מכיסוי (זו הייתה הסיבה האמיתית ל"יומן הדוחות לא מציג הכל").

### תבניות — 15 גלאים, מחולקים לשתי רמות ציון

**רמה 1 — מבניות (רצפה 65-70, עד 99-100):** Cup & Handle (מלא, או ללא ידית), Double Bottom, פריצת שיא 52 שבועות, פריצת התנגדות אופקית, Golden Cross, **פריצת ממוצע 150 יום** (מחיר חוזר מעל הממוצע אחרי שהיה מתחתיו — סימן התאוששות, בהשראת תבנית ANET), Bull Flag, משולש עולה, קפיצה על קו מגמה עולה, **קפיצה על ממוצע 150 יום כתמיכה** (הממוצע עצמו עולה, המחיר נגע בו וקפץ — שונה מקו מגמה אלכסוני), קפיצה על תמיכה אופקית.

**רמה 2 — אינדיקטורים גנריים (תקרה נמוכה, מובטח נמוך מרמה 1):** MACD Crossover (≤60), Momentum Surge (≤58), RSI Bounce (≤55).

**⚠️ תיקון קריטי — "טריות" (freshness check):** שלוש תבניות הפריצה (התנגדות אופקית, משולש עולה, קו מגמה יורד) + פריצת ממוצע 150 **דורשות במפורש** שהסגירה של אתמול עדיין לא חצתה את הרמה, ורק הסגירה של היום חוצה אותה. בלי זה, מניה שפרצה רמה פעם אחת והמשיכה לטפס הייתה מסתמנת מחדש **בכל סריקה** במשך שבועות — זה בדיוק מה שגרם לניפוח ההיסטוריה (ראה סעיף 5).

**דרישת נפח:** חלה על 52w-high, Bull Flag, משולש עולה, Double Bottom, שבירת קו מגמה יורד, ופריצת ממוצע 150. **לא חלה** על התבניות המבוססות תמיכה/התנגדות/כוס/קפיצת ממוצע 150 (הרמה עצמה נחשבת מספיק חזקה).

**דירוג RS** מתאים ציון ב-±8/-15 מעל כל זה.

### יומן דוחות שבועי (חדש)
"מעניינות בלבד" — לא כל היקום, רק **40 השמות הגדולים** (לפי שווי שוק) שמדווחים בחלון של -2/+7 ימים. **עלות תוספתית אפסית ל-yfinance**: משתמש ב-`info.get("earningsTimestamp")` שכבר נשלף לכל טיקר בלולאת הסריקה הרגילה. session (☀️ לפני הפתיחה / 🌙 אחרי הסגירה) נגזר משעון ניו יורק. ברגע שהחברה מדווחת בפועל, נוספות תוצאות EPS **והכנסות** (בפועל מול תחזית, ירוק/אדום לפי הכאה/פספוס) — עוד ~2 קריאות yfinance לכל אחד מ-40 השמות בלבד.

### תיקון קריטי: ניהול חיבור DB (Neon auto-suspend)
`main()` פותח חיבור רק לפני הכתיבה, לא לפני הסריקה, עם retry אוטומטי.

---

## 8. Pipeline B — אגרגטור חדשות

### סיווג כתבות — שוכתב מחדש (`classify_headline`, במקום `matches_high_impact` הישן)
- משתמש ב**כותרת + תקציר RSS יחד**, לא רק כותרת — כותרות לרוב עמומות/קליקבייט.
- הקטגוריה נבחרת לפי **איזו קבוצת מילות מפתח מופיעה הכי הרבה** בטקסט המלא (רמה 1 = משקל כפול), לא "מה שמופיע ראשון" — הישן היה מסווג בטעות כתבות דוחות כ"ריבית" רק כי "Fed" הופיע לפני "earnings".
- **`EXCLUSION_PHRASES`** — רשימה גדלה של ביטויים שחוסמים זיהוי שגוי (heart attack, price war, gold medal, chocolate chip וכו') גם אם מילת מפתח טכנית מופיעה.
- **`GEO_AMBIGUOUS_KEYWORDS`** (War/Conflict/Tension/Crisis/Attack) — היו ברמה 1 בלי סינון בכלל, מה שהציף את קטגוריית "גאופוליטי" בתוכן לא רלוונטי. עכשיו דורשות הקשר כלכלי מפורש (`GEO_CONTEXT_WORDS`: נפט, מכסים, כלכלה, שוק, דולר וכו'), באותו עיקרון שכבר חל על "AI"/"Tech"/שמות חברות. "Sanctions" נשארה ללא סינון (כמעט אף פעם לא דו-משמעית).

### תרגום — חוזק משמעותית
- `translate_to_hebrew`: jitter + ריטריי אחד לכל ספק (Google gtx → MyMemory) לפני מעבר הלאה.
- **`retry_failed_translations()`** — תיקון-עצמי בתחילת כל ריצה: מאתר כתבות מ-72 השעות האחרונות שנתקעו באנגלית (כי `ON CONFLICT DO NOTHING` אומר שהן לעולם לא ינוסו שוב מעצמן), ומנסה שוב.
- **חדשות ספציפיות למניה** (`/api/stock-news/{ticker}`, "חדשות אחרונות" בכרטיס מניה / Deep Dive) — נתיב תרגום חינמי **נפרד**, on-demand (לא בפייפליין המתוזמן), כי זה נשלף בזמן אמת לכל בקשה ולא מתורגם מראש.

### לוח זמנים
כל שעה, ב-17 דק' אחרי השעה (`*/30` התגלה לא אמין ב-GitHub Actions).

---

## 9. Frontend — index.html

### ניווט — 4 טאבים
בית | פעימת השוק | בדיקה היסטורית | ניתוח מניה

### ⚠️ תקלה קריטית שנלמדה: RTL + `left`/`right` יחד
העמוד קבוע `dir="rtl"` (החלפת שפה משנה רק טקסט, לא יותר מחליפה כיוון — זו הייתה בעיה נפרדת שתוקנה: "הכל מחליף צד" כשמחליפים שפה). **גילוי חשוב:** אלמנט עם `left:0` **וגם** `right:0` ב-CSS, ועם `left` מוגדר גם inline ב-JS — ב-RTL הדפדפן **מתעלם מה-`left`** ופותר את המיקום לפי `right` בלבד (per spec, over-constrained box). זה גרם לתוויות הסקטורים במפת החום להיות מוצמדות תמיד לקצה הימני, בלי קשר למיקום האמיתי. **תיקון:** לעולם לא להגדיר גם `left` וגם `right` יחד על אותו אלמנט כשה-`left` נקבע דינמית ב-JS.

### מפת חום — treemap דו-שכבתי, מקובץ לפי סקטור
שכבה ראשונה: תיבות סקטור (GICS) בגודל לפי שווי שוק מצטבר, עם כותרת וגבול (רווח אמיתי של 3px בין תיבות — גבול משותף של 1px לבד "נעלם" ויזואלית כשיש צבעים בשני הצדדים). שכבה שנייה: טריטמאפ פנימי של המניות בתוך כל תיבת סקטור, לפי שווי שוק אינדיבידואלי.

### עמוד "פעימת השוק" — יומן דוחות חדש
מתחת למדד הפחד-תאווה: **"יומן דוחות השבוע"** — מקובץ לפי יום (היום/מחר/יום בשבוע), ממוין בכל יום לפי session (☀️ לפני הפתיחה קודם, 🌙 אחרי הסגירה אחרון), אייקון session ליד הטיקר. ברגע שיש תוצאות בפועל — שורת EPS + הכנסות, ירוק/אדום לפי הכאה/פספוס.

### עמוד "בדיקה היסטורית" — סודר וסונן
טבלת "השוואת תבניות" + פילוח חודשי/רבעוני (מהסבב הקודם), אבל:
- **שמות תבניות ידידותיים דו-לשוניים** (`PATTERN_LABELS`/`patternLabel()`) בכל מקום — לא יותר `ma150_support_bounce` גולמי.
- פילטר התבניות הפך מ-16 כפתורים לתפריט נפתח (`<select>`) אחד, ממוין.
- **equity curve משתמש ב-10% מההון לכל איתות**, לא all-in רציף — full-stake על 1000+ איתותים משולבים יצר "קריסה" מלאכותית מוגזמת מווריאנס רגיל. הערה קבועה מתחת לגרף מסבירה את ההנחה.

### עמוד הסריקה הראשי (בית) — פילטר תבניות חדש
תפריט נפתח חדש מעל רשימת המניות שמראה **רק** את התבניות שבאמת מופיעות כרגע בתוצאות — לא היה קיים קודם.

### חדשות — פילטר קטגוריות "בחר מה להציג" (persisted)
לא mute-list — לחיצה על קטגוריה **מציגה** אותה (בחירה מרובה, ריק = הצג הכל), נשמר ב-localStorage בין ביקורים.

### מובייל — תיקוני עומס (max-width:480px)
- כרטיסי הסריקה (`.stock-row`) — גופן/רווחים מוקטנים.
- יומן דוחות — אייקון ☀️/🌙 קומפקטי במקום טקסט "לפני הפתיחה"/"אחרי הסגירה".
- בדיקה היסטורית — שורת 4 הכרטיסים (איתותים/הצלחה/תשואה/ירידה מקסימלית) עוברת מ-1×4 ל-2×2.

### עמוד "ניתוח מניה" (Deep Dive)
ללא שינוי מהסבב הקודם — P/E NTM/LTM זה לצד זה, מניה מול סקטור/תת-סקטור, פונדמנטלס, תיאור Wikipedia/SEC, השוואת עד 3 מניות. **נוסף:** ממוצע 150 יום אדום על הגרף (ראה סעיף 4).

### תיקון אמין: retry על כל קריאות ה-API
ללא שינוי — retry עם המתנה גדלה + fetch מקביל בכל מקום רלוונטי.

---

## 10. החלטות ארכיטקטורה מרכזיות

| החלטה | סיבה |
|---|---|
| Neon + Render + GitHub Actions (לא Railway) | Railway היה תקופת ניסיון בתשלום |
| ריפו GitHub ציבורי | דקות Actions ללא הגבלה; הסודות עדיין מוגנים |
| GitHub Actions בתדירות נמוכה + 2 טריגרים לסשן | `schedule` לא אמין; גיבוי קרוב בזמן במקום תדירות גבוהה שכבר הוכחה לא אמינה |
| `build_scan_universe()` מסובב סדר לפי יום | חסימת Yahoo תמיד פגעה באותם טיקרים "בסוף" הרשימה הקבועה |
| `.history()` ראשי, `.info`/SEC/Wikipedia כ-fallback מדורג | `.info` נחסם לעיתים קרובות מ-IP ענן |
| תבניות מבניות > אינדיקטורים גנריים בציון, מובטח מתמטית | המשתמש הוא בעיקר אנליסט טכני |
| כל תבניות הפריצה דורשות freshness check | מניעת ניפוח היסטורי מריבוי-סימון של אותו איתות ישן |
| יומן דוחות: 40 שמות בלבד, לא כל היקום | "מעניינות" בלבד, עלות yfinance אפסית/מזערית |
| equity curve ב-10% מההון לכל איתות | full-stake רציף על 1000+ איתותים יוצר קריסה מלאכותית |
| סינון חדשות: כותרת+תקציר, שקלול תדירות, לא AI | חינמי בלבד עד שהאתר יהפוך למוצר אמיתי |
| פילטר קטגוריות חדשות = "הצג רק את זה" | לא mute-list — המשתמש מעדיף לבחור מה כן לראות |
| חיבור DB נפתח רק ממש לפני כתיבה | Neon auto-suspend |

---

## 11. פיצ'רים שהושלמו ✅ (מצטבר)

- מעבר תשתית מלא Railway → Neon+Render+GitHub Actions
- עמודי "בדיקה היסטורית" ו"ניתוח מניה" מלאים; טבלת השוואת תבניות + פילוח חודשי/רבעוני
- מפת חום כ-treemap אמיתי, **ומקובץ לפי סקטור GICS** (דו-שכבתי) — כולל תיקון RTL קריטי
- **15 תבניות טכניות** — כולל 2 חדשות סביב ממוצע 150 יום (פריצה + קפיצת תמיכה)
- **ממוצע 150 יום אדום** על כל גרפי TradingView
- **תיקון freshness-check** לכל תבניות הפריצה — מונע ניפוח היסטורי
- **יומן דוחות שבועי** — תאריכים, session, ולאחר דיווח: EPS + הכנסות בפועל מול תחזית
- **שכתוב מלא של סיווג החדשות** — כותרת+תקציר, שקלול תדירות, רשימת חריגים, גאופוליטי מסונן
- **תרגום חינמי מחוזק** — jitter+retry, תיקון-עצמי לכתבות תקועות, תרגום on-demand לחדשות ספציפיות למניה
- **פילטר קטגוריות חדשות** — בחירה מרובה persisted, "הצג רק את זה"
- **פילטר תבניות** — גם בסריקה הראשית וגם בבדיקה ההיסטורית, שמות ידידותיים דו-לשוניים
- **`cleanup_stale_signals.py`** — נוקה 82% מההיסטוריה הישנה שהייתה מנופחת
- **תיקוני RTL/תרגום כלליים** — שפה לא הופכת יותר כיוון פריסה; ~20 מחרוזות UI קבועות שלא תורגמו
- **תיקוני מובייל** — כרטיסי סריקה, יומן דוחות, שורת סטטיסטיקות בדיקה היסטורית
- NTM/LTM P/E זה לצד זה; תיאור חברה מ-Wikipedia; השוואת סקטור/תת-סקטור מחדש; השוואת עד 3 מניות
- תיקון קריטי CORS; `sec_cik_cache` ב-Postgres

---

## 12. מגבלות ידועות ⚠️

| מגבלה | פירוט |
|---|---|
| **`.info` של yfinance לא אמין מ-Render** | חסום/מוגבל מ-IP ענן. פתרון מדורג: `.history()` → SEC EDGAR → Wikipedia → CSV S&P500. |
| **GitHub Actions `schedule` לא מדויק** | סטיות אפשריות, ממוזג חלקית ע"י 2 טריגרים לסשן. נדחה פתרון חיצוני (cron-job.org) ביודעין. |
| **Neon auto-suspend + branches** | כל קוד עתידי חייב לפתוח חיבור DB בסמוך לכתיבה. |
| **Render Free נרדם** | `keep-render-awake.yml` לא מובטח 100%. תקלה שנראית כמו "עדיין ישן" עלולה להיות בעצם CORS/deploy תקוע — לבדוק Console בדפדפן. |
| **NTM P/E** | תלוי בתחזית אנליסטים (`.info` בלבד) — יכול להישאר "לא זמין"; LTM (SEC-based) תמיד אמין יותר. |
| **`revenue_estimate` ביומן דוחות** | "קופא" בכוונה ברגע שנתפס לראשונה — ראה סעיף 5 (COALESCE). |
| Backtest/פילוח תקופתי ריקים בהתחלה | דורש 14-28+ יום שהאיתותים "יבשילו". |
| VIX | הוסר לגמרי, אין תוכנית לחזור |
| תרגום חינמי (Google gtx/MyMemory), CNN Fear & Greed לא-רשמי | לא-רשמיים, יכולים להישבר בלי התראה — עכשיו עם retry+תיקון-עצמי אבל עדיין לא מובטח 100% |
| סיווג חדשות מבוסס מילות מפתח (לא AI) | דיוק מוגבל מטבעו לעומת מודל שפה; זו החלטה מודעת כדי להישאר חינמי |

---

## 13. TODO עתידי 🔮

- [ ] **טבלת watchlist מלאה** (score/P/E/RS Rating לכל מניות המעקב יחד) — נדחה במפורש, "אולי בהמשך"
- [ ] **סיווג חדשות מבוסס AI** (Anthropic/OpenAI API, לא מנוי) — מדויק משמעותית אבל עולה כסף; נדחה עד שהאתר יהפוך למוצר אמיתי
- [ ] פילוח הכנסות לפי תחום עסקי — דורש מקור בתשלום
- [ ] Alerts באימייל/webhook
- [ ] מדריך נרות יפניים ל"פעימת השוק"
- [ ] Push notifications לפריצות
- [ ] עתיד רחוק (רק אם עוברים למוצר רב-משתמשים): הרשמה/התחברות, רישוי מסחרי לנתוני Yahoo, rate limiting, בדיקות אוטומטיות (pytest), עמוד "בריאות מערכת", ניטור/התראות כשל

---

## 14. איך לפתוח שיחה חדשה

1. צרף קובץ זה (`CLAUDE.md`)
2. אין צורך להעלות קבצי קוד ידנית — בקש מ-Claude למשוך את הגרסאות העדכניות **ישירות מ-GitHub** (`raw.githubusercontent.com/sabatonissim/swing-dashboard/main/...`): `index.html`, `api_server.py`, `pipeline_a_scanner.py`, `pipeline_b_news_aggregator.py`. זה מבטיח עבודה על מה שבאמת פרוס, לא עותק מקומי ישן.
3. תאר מה אתה רוצה לשנות
4. בסיום שינוי משמעותי: בקש "עדכן את CLAUDE.md"

---

*עדכון אחרון: ספטמבר 2026 | גרסה: v4 — יומן דוחות + הכנסות, מפת חום לפי סקטור, ניקוי היסטוריה (82%), שכתוב סיווג חדשות, MA150 (תבניות+גרף), פילטרי תבניות/קטגוריות, תיקוני RTL/מובייל*
